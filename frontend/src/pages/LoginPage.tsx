export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50 p-4">
      <div className="w-full max-w-sm">
        <h1 className="text-2xl font-bold text-brand-primary mb-6 text-center">Login</h1>
        {/* TODO: Email + Password form */}
        {/* TODO: Submit → POST /auth/login */}
        {/* TODO: Store JWT, redirect to home */}
        {/* TODO: Link to register */}
      </div>
    </div>
  );
}
