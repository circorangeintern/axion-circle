export default function AdminPage() {
  return (
    <div className="min-h-screen bg-gray-50 p-4">
      <h1 className="text-2xl font-bold text-brand-primary mb-4">Admin Dashboard</h1>
      {/* TODO: Report table (sortable, filterable) */}
      {/* TODO: Status update button + modal with note */}
      {/* TODO: Stats summary (pending, resolved today, avg time) */}
      {/* TODO: Reward management */}
    </div>
  );
}
